import { css } from '@emotion/react';
import Layout from '@/components/Layout';

const Home = () => {
  return (
    <Layout>
      <main>
        <p>todo block</p>
      </main>
    </Layout>
  );
};

export default Home;
